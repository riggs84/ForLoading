import java.util.ArrayList;

public class ThreadsRunner {
    /*public static void main(String[] args) {
        String url = args[0]; //url
        String comp = args[1]; // comp id
        String job = args[2]; // job id
        int thread = Integer.parseInt(args[3]); // amount of threads
        ArrayList<User> threads = new ArrayList<>();
        for (int i = 0; i <= thread; i++) {
            threads.add(new User(url, comp, job));
            //threads.get(i).run("1687", "4546");
            threads.get(i).start();
        }
    }*/
}
